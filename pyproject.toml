[tool.poetry]
name = "auto-push"
version = "0.1.0"
description = ""
authors = ["Ugolin Ollé <hello@ugolin-olle.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
